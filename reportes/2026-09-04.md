# Reporte nocturno — 2026-09-04

## Titular

Noche 24 seguida sin datos nuevos: el entorno en la nube sigue sin poder
llegar a `api.mercadopublico.cl`. Mismo bloqueo de política de egreso de red
que las veintitrés noches anteriores (12 de agosto al 3 de septiembre),
mensaje de rechazo idéntico, letra por letra, al de anoche.

## Lo accionable ahora

Nada de licitaciones: sin acceso a la API no hay llamados que recolectar ni
oportunidades nuevas que evaluar. Las 20 oportunidades ya registradas (10
`analizada`, 9 `descartada`, 1 `ofertada`) siguen exactamente donde estaban
hace veinticuatro noches.

Lo único accionable sigue siendo de infraestructura: agregar
`api.mercadopublico.cl` al allowlist de egreso de red del entorno en
[claude.ai/code/routines](https://claude.ai/code/routines)
(`trig_01TcKRcAxJJAfCwXrVPrPMLu`). Nadie con acceso a esa configuración la ha
tocado en veinticuatro noches, a juzgar por que el rechazo persiste idéntico.
No hay notificación aparte esta noche: no hay hito nuevo que marcar (el de
las tres semanas ya se avisó el 2026-09-02) y el bloqueo no cambió de forma.

## Por qué

No aplica a licitaciones: no se evaluó ninguna oportunidad nueva porque no
hubo datos nuevos que procesar.

## Descartes

Ninguno nuevo.

## Qué falta confirmar

- **El bloqueo es exactamente el mismo que anoche, sin variación.** El
  `CONNECT` directo a `api.mercadopublico.cl:443` (leído a nivel de socket,
  porque `curl` no expone el cuerpo cuando el túnel falla) devuelve:

  ```
  HTTP/1.1 403 Forbidden
  Content-Type: text/plain; charset=utf-8
  X-Content-Type-Options: nosniff
  Content-Length: 79

  request blocked: no rule or allowlist entry allows host "api.mercadopublico.cl"
  ```

  Byte por byte igual al de anoche (2026-09-03). El diagnóstico del proxy
  (`$HTTPS_PROXY/__agentproxy/status`) tampoco cambió de forma relevante:
  `recentRelayFailures` vino vacío esta vez (a diferencia de anoche, que
  traía una entrada `connect_rejected` de la corrida misma — variación de
  cuándo se consulta el estado, no del bloqueo), `installedProxyPreconfiguredClis`
  sigue vacío y `selective: false`. El bloqueo sigue ocurriendo en una capa
  de política de red del entorno, distinta al proxy mismo, y nadie la ha
  tocado en veinticuatro noches.
  - **Se corrió igual `mpagente demanda --meses 2 --hilos 4`** (con
    `timeout 100`): agotó reintentos contra `listaOCDSAgnoMes/2026/09/0/500`
    y `.../2026/08/0/500`, página vacía en ambos, 0 llamados guardados.
    Mismo resultado que las veintitrés noches anteriores.
- Sigue sin resolverse la brecha de tests reportada las veintitrés noches
  anteriores: con solo `.[bases]` instalado, `pytest` ni siquiera está
  disponible (`No module named pytest`) porque `dev` es un extra aparte; con
  `.[dev,bases]` instalado (en la práctica, `pytest` + `pytest-cov` sobre
  `.[bases]`), 58 pasan, 6 fallan y 6 dan error por
  `FileNotFoundError: data/mock/licitaciones.json`, un fixture en
  `.gitignore` que nunca se commiteó. No relacionado con el bloqueo de red
  — solo confirma que un clon fresco no lo trae. Afecta solo al modo
  "licitaciones abiertas", en pausa según `CLAUDE.md`; no afecta demanda ni
  nichos. Sigue pendiente decidir si se commitea el fixture o se documenta
  como gap conocido.

## Estado del sistema

- **Contexto importado correctamente**: 314 familias, 20 oportunidades, 22
  eventos (generado 2026-09-03T06:10:07 según el `contexto.json` del repo —
  el timestamp interno no cambia porque no hubo datos nuevos que agregar).
- **Embudo** (sin cambios): `analizada` 10 · `descartada` 9 · `ofertada` 1.
  Ofertadas 1, ganadas 0.
- **Garantías e instrumentos de financiamiento registrados**: 0.
- **Proveedores registrados**: 0.
- **Diagnóstico de red**: `CONNECT api.mercadopublico.cl:443` vía el proxy
  configurado → `HTTP/1.1 403 Forbidden`, cuerpo `request blocked: no rule
  or allowlist entry allows host "api.mercadopublico.cl"`. Idéntico, byte a
  byte, al de anoche. Veinticuatro noches seguidas con el mismo bloqueo de
  fondo.
- **`mpagente demanda`**: se corrió completa (con `timeout 100` en vez de
  esperar los ~90 minutos), agotó reintentos en ambos meses (403 en cada
  petición) y guardó 0 llamados.
- **`mpagente pidiendo`**: confirmó "No hay llamados recolectados"; el
  resultado esperado dado que `demanda` reportó 0 llamados. La base de
  96 MB del Mac tampoco está disponible en este entorno.
- **Tests**: `.venv/bin/python -m pytest -q` → 58 pasan, 6 fallan, 6 dan
  error, todos por el fixture `data/mock/licitaciones.json` ausente. Mismo
  resultado que las veintitrés noches anteriores.
- **Export de contexto**: se re-exporta igual al final, solo cambia la
  marca de tiempo, para no perder el histórico ya cargado.
