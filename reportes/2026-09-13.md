# Reporte nocturno — 2026-09-13

## Titular

Noche 33 seguida sin datos nuevos: el entorno en la nube sigue sin poder
llegar a `api.mercadopublico.cl`. El rechazo es el mismo, byte a byte, que
hace un mes.

## Lo accionable ahora

Nada de licitaciones: sin acceso a la API no hay llamados que recolectar ni
oportunidades nuevas que evaluar. Las 20 oportunidades ya registradas (10
`analizada`, 9 `descartada`, 1 `ofertada`) siguen exactamente donde estaban
hace treinta y tres noches.

Lo único accionable sigue siendo de infraestructura: agregar
`api.mercadopublico.cl` al allowlist de egreso de red del entorno en
[claude.ai/code/routines](https://claude.ai/code/routines)
(`trig_01TcKRcAxJJAfCwXrVPrPMLu`). Nadie con acceso a esa configuración la ha
tocado en treinta y tres noches, a juzgar por que el rechazo persiste idéntico.

No corresponde notificación aparte esta noche: es la trigésima tercera
repetición sin variación de un problema ya avisado en múltiples oportunidades
anteriores.

## Por qué

No aplica a licitaciones: no se evaluó ninguna oportunidad nueva porque no
hubo datos nuevos que procesar.

## Descartes

Ninguno nuevo.

## Qué falta confirmar

- **El bloqueo es exactamente el mismo que las treinta y dos noches
  anteriores.** El `CONNECT` a `api.mercadopublico.cl:443` a través del
  proxy de egreso configurado devuelve:

  ```
  curl: (56) CONNECT tunnel failed, response 403
  ```

  El diagnóstico del proxy (`$HTTPS_PROXY/__agentproxy/status`) reporta el
  motivo explícito: `connect_rejected (the egress proxy denied the CONNECT
  (organization policy) or could not reach the destination)`. El proxy
  mismo sigue con `enabled: true`, `selective: false`,
  `installedProxyPreconfiguredClis: []`, y `api.mercadopublico.cl` sigue sin
  aparecer en la lista `noProxy` (que solo cubre `api.anthropic.com`,
  registros de paquetes y rangos internos). El bloqueo ocurre en una capa de
  política de organización, distinta del proxy mismo, y nadie la ha tocado
  en treinta y tres noches.
  - **Se corrió igual `mpagente demanda --meses 2 --hilos 4`** (con
    `timeout 100`): agotó reintentos contra `listaOCDSAgnoMes/2026/09/0/500`
    y `.../2026/08/0/500`, página vacía en ambos, 0 llamados guardados.
    Mismo resultado que las treinta y dos noches anteriores.
- Sigue sin resolverse la brecha de tests reportada las treinta y dos noches
  anteriores: con `.[dev,bases]` instalado en un venv fresco, 58 pasan, 6
  fallan y 6 dan error por `FileNotFoundError: data/mock/licitaciones.json`,
  un fixture en `.gitignore` que nunca se commiteó. No relacionado con el
  bloqueo de red — solo confirma que un clon fresco no lo trae. Afecta solo
  al modo "licitaciones abiertas", en pausa según `CLAUDE.md`; no afecta
  demanda ni nichos. Sigue pendiente decidir si se commitea el fixture o se
  documenta como gap conocido.

## Estado del sistema

- **Contexto importado correctamente**: 314 familias, 20 oportunidades, 22
  eventos (generado 2026-09-12T06:07:58 según el `contexto.json` del repo —
  el timestamp interno no cambia porque no hubo datos nuevos que agregar).
- **Embudo** (sin cambios): `analizada` 10 · `descartada` 9 · `ofertada` 1.
  Ofertadas 1, ganadas 0.
- **Garantías e instrumentos de financiamiento registrados**: 0.
- **Proveedores registrados**: 0.
- **Diagnóstico de red**: `CONNECT api.mercadopublico.cl:443` vía el proxy
  configurado → `403` por política de organización, idéntico en sustancia a
  las treinta y dos noches anteriores. Treinta y tres noches seguidas con el
  mismo bloqueo de fondo.
- **`mpagente demanda`**: se corrió completa (con `timeout 100` en vez de
  esperar los ~90 minutos), agotó reintentos en ambos meses (403 en cada
  petición) y guardó 0 llamados.
- **`mpagente pidiendo`**: confirmó "No hay llamados recolectados"; el
  resultado esperado dado que `demanda` reportó 0 llamados.
- **Tests**: `.venv/bin/python -m pytest -q` → 58 pasan, 6 fallan, 6 dan
  error, todos por el fixture `data/mock/licitaciones.json` ausente. Mismo
  resultado que las treinta y dos noches anteriores.
- **Export de contexto**: se re-exporta igual al final, solo cambia la
  marca de tiempo, para no perder el histórico ya cargado.
