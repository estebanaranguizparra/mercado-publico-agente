# Reporte nocturno — 2026-08-19

## Titular

Octava noche seguida sin datos nuevos: el entorno en la nube sigue sin poder
llegar a `api.mercadopublico.cl`. Mismo bloqueo de política de egreso de las
siete noches anteriores (12 al 18 de agosto), sin ningún cambio.

## Lo accionable ahora

Nada. Sin acceso a la API no hay llamados que recolectar ni oportunidades
nuevas que evaluar. Las 20 oportunidades ya registradas (10 `analizada`, 9
`descartada`, 1 `ofertada`) siguen exactamente donde estaban hace ocho noches.

## Por qué

No aplica: no se evaluó ninguna oportunidad nueva porque no hubo datos nuevos
que procesar.

## Descartes

Ninguno nuevo.

## Qué falta confirmar

- **El bloqueo lleva ocho noches seguidas, sin cambio en la política.**
  `curl -v https://api.mercadopublico.cl/...` sigue devolviendo `CONNECT
  tunnel failed, response 403`. El diagnóstico del proxy
  (`curl -sS "$HTTPS_PROXY/__agentproxy/status"`) lo confirma explícitamente:

  ```json
  {
    "ts": "2026-08-19T06:08:52.952Z",
    "kind": "connect_rejected",
    "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
    "host": "api.mercadopublico.cl:443"
  }
  ```

  Se intentó igualmente correr `mpagente demanda --meses 2 --hilos 4` (no
  solo el diagnóstico directo) para confirmar que no era un rechazo aislado
  del `curl`: el comando se colgó sin progreso — sin salida por el problema ya
  conocido de `rich` con salida redirigida — y el reintento generó seis
  rechazos `403` más en `recentRelayFailures`, todos contra el mismo host.
  Es una **denegación de política de egreso** del entorno, no un problema
  transitorio ni del lado de la API de ChileCompra.
- **Acción pendiente, sin cambios respecto de los siete reportes
  anteriores**: `api.mercadopublico.cl` necesita agregarse a la lista de
  hosts permitidos en la política de red del entorno de esta rutina
  (`claude.ai/code/routines`, `trig_01TcKRcAxJJAfCwXrVPrPMLu`). Esta rutina
  no tiene forma de cambiar esa política desde adentro. El próximo paso lo
  tiene que dar una persona con acceso a esa configuración, no otra corrida
  de este agente. Con ocho noches consecutivas del mismo bloqueo, vale la
  pena que alguien revise si la rutina debería pausarse hasta que se resuelva
  la política, en vez de seguir corriendo cada noche sin poder producir nada.
- Sigue sin resolverse la brecha de tests reportada las siete noches
  anteriores: 58 pasan, 6 fallan y 6 dan error por `FileNotFoundError:
  data/mock/licitaciones.json`, un fixture en `.gitignore` que nunca se
  commiteó. No relacionado con el bloqueo de red — solo confirma que un clon
  fresco no lo trae. Afecta solo al modo "licitaciones abiertas", en pausa
  según `CLAUDE.md`; no afecta demanda ni nichos. Sigue pendiente decidir si
  se commitea el fixture o se documenta como gap conocido.

## Estado del sistema

- **Contexto importado correctamente**: 314 familias, 20 oportunidades, 22
  eventos (generado 2026-08-18T06:09:06 según el commit previo, sin cambios
  desde hace siete noches porque no hubo datos nuevos que agregar).
- **Embudo** (sin cambios): `analizada` 10 · `descartada` 9 · `ofertada` 1.
  Ofertadas 1, ganadas 0.
- **Garantías e instrumentos de financiamiento registrados**: 0.
- **Proveedores registrados**: 0.
- **Diagnóstico de red**: `curl -v https://api.mercadopublico.cl/...` →
  `CONNECT tunnel failed, response 403`. `mpagente demanda` se intentó y
  quedó colgado sin progreso contra el mismo bloqueo; se cortó manualmente
  en vez de esperar los ~90 minutos completos, porque el resultado ya estaba
  determinado.
- **`mpagente pidiendo`**: no se ejecutó; sin llamados recolectados no
  habría nada que mostrar (la base de 96 MB del Mac tampoco está disponible
  en este entorno).
- **Tests**: `.venv/bin/python -m pytest -q` → 58 pasan, 6 fallan, 6 dan
  error, todos por el fixture `data/mock/licitaciones.json` ausente. Mismo
  resultado que las siete noches anteriores.
- **Export de contexto**: se re-exporta igual al final, solo cambia la marca
  de tiempo, para no perder el histórico ya cargado.
