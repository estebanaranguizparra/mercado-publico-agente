# Reporte nocturno — 2026-08-13

## Titular

Segunda noche seguida sin datos nuevos: el entorno en la nube sigue con
`mercadopublico.cl` bloqueado a nivel de proxy de salida, igual que anoche
(2026-08-12). No es el límite de tasa de 5 req/s ya conocido — es un rechazo
`403` antes de llegar a la API, para cualquier solicitud, a cualquier subdominio.

## Lo accionable ahora

Nada. Sin acceso a la API no hay llamados que recolectar ni oportunidades
nuevas que evaluar. Las 20 oportunidades ya registradas (10 `analizada`, 9
`descartada`, 1 `ofertada`) siguen exactamente donde estaban anoche.

## Por qué

No aplica: no se evaluó ninguna oportunidad nueva porque no hay datos nuevos.

## Descartes

Ninguno nuevo.

## Qué falta confirmar

- **El bloqueo de red lleva ya dos noches seguidas.** Ayer se documentó como
  posible error de configuración a corregir; hoy se repite sin cambios, lo
  que sugiere que nadie ha tocado la política de egreso del entorno todavía.
  Verificación de hoy, idéntica a la de ayer:
  - `curl -v https://api.mercadopublico.cl/...` → `CONNECT tunnel failed,
    response 403` (el proxy corta la conexión antes de que llegue a la API).
  - `mpagente demanda --meses 2 --hilos 4` corrido en vivo: agotó reintentos
    en **ambos** meses (2026-07 y 2026-08), incluyendo julio, que en un
    entorno con red abierta sí debería responder (agosto en curso sí se
    espera que falle, julio no). 0 procesos revisados, 0 llamados guardados.
- **Esta rutina no puede avanzar mientras el bloqueo siga activo.** `demanda`,
  `recolectar`, `lector-bases` y los buscadores de proveedores dependen todos
  de red hacia `mercadopublico.cl`. Revisar y ajustar la política de red del
  entorno en `claude.ai/code/routines` (configuración del entorno de esta
  rutina, `trig_01TcKRcAxJJAfCwXrVPrPMLu`) es el único paso pendiente; no hay
  nada más que este agente pueda intentar desde acá.
- Sigue sin resolverse la brecha de tests ya reportada ayer: 58 pasan, 6
  fallan y 6 dan error por `FileNotFoundError: data/mock/licitaciones.json`,
  un fixture que está en `.gitignore` y nunca se commiteó. No relacionado con
  el bloqueo de red; solo confirma que un clon fresco no lo trae.

## Estado del sistema

- **Contexto importado correctamente**: 314 familias, 20 oportunidades, 22
  eventos (generado 2026-08-12T06:10:38, sin cambios desde ayer porque la
  corrida de anoche tampoco pudo agregar nada).
- **Embudo** (sin cambios): `analizada` 10 · `descartada` 9 · `ofertada` 1.
  Ofertadas 1, ganadas 0.
- **Garantías e instrumentos de financiamiento registrados**: 0.
- **Proveedores registrados**: 0.
- **`mpagente demanda --meses 2 --hilos 4`**: 0 procesos revisados, 0
  llamados guardados, ambos meses fallaron por el bloqueo de red.
- **`mpagente pidiendo`**: "No hay llamados recolectados" — consistente,
  este entorno no trae la base de 96 MB del Mac.
- **Tests**: `.venv/bin/python -m pytest -q` → 58 pasan, 6 fallan, 6 dan
  error, todos por el fixture `data/mock/licitaciones.json` ausente.
- **Export de contexto**: se re-exporta igual al final, solo cambia la marca
  de tiempo, para no perder el histórico ya cargado.
