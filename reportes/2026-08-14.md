# Reporte nocturno — 2026-08-14

## Titular

Tercera noche seguida sin datos nuevos: el entorno en la nube sigue sin poder
llegar a `api.mercadopublico.cl`. Es el mismo bloqueo de las dos noches
anteriores (12 y 13 de agosto), no el límite de tasa de 5 req/s ya conocido.
Hoy se confirmó con el diagnóstico del proxy del propio entorno que es un
rechazo de política, no un problema transitorio de la API.

## Lo accionable ahora

Nada. Sin acceso a la API no hay llamados que recolectar ni oportunidades
nuevas que evaluar. Las 20 oportunidades ya registradas (10 `analizada`, 9
`descartada`, 1 `ofertada`) siguen exactamente donde estaban hace dos noches.

## Por qué

No aplica: no se evaluó ninguna oportunidad nueva porque no hubo datos nuevos
que procesar.

## Descartes

Ninguno nuevo.

## Qué falta confirmar

- **El bloqueo lleva tres noches seguidas y hoy quedó identificado con
  precisión.** El diagnóstico del proxy de salida de este entorno
  (`curl -sS "$HTTPS_PROXY/__agentproxy/status"`) registra explícitamente:

  ```
  "recentRelayFailures": [{
    "ts": "2026-08-14T06:14:53.325Z",
    "kind": "connect_rejected",
    "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
    "host": "api.mercadopublico.cl:443"
  }]
  ```

  Esto es una **denegación de política de egreso** del entorno en el que
  corre esta rutina, no un error de configuración del agente ni un límite de
  la API de ChileCompra. La documentación del propio proxy (`/root/.ccr/README.md`)
  es explícita: un 403 en el `CONNECT` es una política de organización y no
  se debe reintentar ni rodear, sino reportar el host bloqueado.
- **Acción pendiente, y quién puede tomarla**: `api.mercadopublico.cl` (y
  probablemente todo `mercadopublico.cl`) necesita agregarse a la lista de
  hosts permitidos en la política de red del entorno de esta rutina, en la
  configuración del entorno donde corre (`claude.ai/code/routines`,
  `trig_01TcKRcAxJJAfCwXrVPrPMLu`). Esta rutina no tiene forma de cambiar esa
  política desde adentro — es justamente lo que el proxy impide.
- Verificación de hoy: `mpagente demanda --meses 2 --hilos 4` no se ejecutó
  porque el `curl` de diagnóstico directo a la API ya devolvía el mismo `403`
  de `CONNECT tunnel failed` de las dos noches previas; correr `demanda` solo
  habría repetido el mismo resultado (0 procesos, 0 llamados) y consumido
  ~90 minutos de reintentos sin nueva información.
- Sigue sin resolverse la brecha de tests ya reportada las dos noches
  anteriores: 58 pasan, 6 fallan y 6 dan error por
  `FileNotFoundError: data/mock/licitaciones.json`, un fixture que está en
  `.gitignore` y nunca se commiteó. No relacionado con el bloqueo de red;
  solo confirma que un clon fresco no lo trae. Si esto se sigue reportando
  noche tras noche sin corregirse, conviene decidir explícitamente si se
  commitea el fixture o se documenta como gap conocido para dejar de
  repetirlo en cada reporte.

## Estado del sistema

- **Contexto importado correctamente**: 314 familias, 20 oportunidades, 22
  eventos (generado 2026-08-13T06:19:27, sin cambios desde hace dos noches
  porque no hubo datos nuevos que agregar).
- **Embudo** (sin cambios): `analizada` 10 · `descartada` 9 · `ofertada` 1.
  Ofertadas 1, ganadas 0.
- **Garantías e instrumentos de financiamiento registrados**: 0.
- **Proveedores registrados**: 0.
- **Diagnóstico de red**: `curl -v https://api.mercadopublico.cl/...` →
  `CONNECT tunnel failed, response 403`. El endpoint de estado del proxy
  (`__agentproxy/status`) confirma que es `policy denial`, con
  `api.mercadopublico.cl:443` nombrado explícitamente como host rechazado.
  `mpagente demanda` no se corrió porque el resultado ya estaba determinado
  por este bloqueo.
- **`mpagente pidiendo`**: no se ejecutó; sin llamados recolectados no habría
  nada que mostrar (la base de 96 MB del Mac tampoco está disponible en este
  entorno).
- **Tests**: `.venv/bin/python -m pytest -q` → 58 pasan, 6 fallan, 6 dan
  error, todos por el fixture `data/mock/licitaciones.json` ausente. Mismo
  resultado que las dos noches anteriores.
- **Export de contexto**: se re-exporta igual al final, solo cambia la marca
  de tiempo, para no perder el histórico ya cargado.
