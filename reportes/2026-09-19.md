# Reporte nocturno — 2026-09-19

Noche 39 del mismo bloqueo de red. Sin llamados nuevos que analizar; el
embudo de oportunidades queda igual que ayer. Se recuperaron, eso sí, dos
reportes de noches anteriores que no habían llegado a `origin/main`.

## 1. Lo accionable ahora

Sin novedades. No hay llamados recolectados esta noche (`mpagente demanda`
devolvió 0 por el bloqueo de red — ver más abajo), así que no hay nada nuevo
que triage pueda evaluar. Las oportunidades `analizada` de noches anteriores
(9 en total: discos duros, radiocomunicación, unidades de datos, TI/ISP,
software y licencias, impresoras, formación y capacitación, asesorías de
gestión, producción de eventos, gasfitería) siguen abiertas para retomar en
cuanto la red funcione; no se volvieron a evaluar porque no hay datos nuevos
que cambien su motivo registrado.

## 2. Por qué (nada que analizar)

No aplica: cero llamados nuevos, cero bases leídas, cero decisiones nuevas.

## 3. Descartes

Ninguno nuevo. Los 9 descartes previos (telefonía móvil, internet, seguros,
tres licitaciones LR por monto de garantía, una por plazo insuficiente de 7
días, una por barrera dura de fabricante) siguen vigentes con su motivo
original; no hay información nueva que los reabra.

## 4. Qué falta confirmar

- **El bloqueo de red de fondo**, que ya lleva 39 noches seguidas y es lo
  único que impide que este reporte tenga contenido nuevo. Ver diagnóstico
  abajo. Esto ya no es una falla puntual de una noche: es una condición
  estructural del entorno que necesita que alguien con acceso a la
  configuración de red del entorno (no este agente, que corre dentro de él)
  agregue `api.mercadopublico.cl` a la política de salida. El diagnóstico
  técnico exacto está en la sección de estado del sistema, para quien lo
  resuelva no tenga que reconstruirlo.
- El precio de referencia y la competencia que arrastra `contexto.json`
  siguen firmes en su fecha de origen (histórico de adjudicaciones parcial,
  como ya advierte `CLAUDE.md`), pero llevan 39 días sin poder refrescarse
  porque nada nuevo entra por la API mientras el bloqueo siga.
- La brecha de tests por el fixture `data/mock/licitaciones.json` ausente
  (ver estado del sistema) sigue sin decidirse: ¿se commitea el fixture o se
  documenta como gap conocido del modo "licitaciones abiertas" (en pausa)?
  No urgente, pero acumula noches sin resolverse.

## 5. Estado del sistema

- **Contexto importado correctamente**: 314 familias, 20 oportunidades, 22
  eventos (generado 2026-09-18T06:08:12 según el `contexto.json` recuperado
  de anoche — el timestamp interno no cambia porque no hubo datos nuevos que
  agregar).
- **Embudo** (sin cambios): `analizada` 10 · `descartada` 9 · `ofertada` 1.
  Ofertadas 1, ganadas 0.
- **Garantías e instrumentos de financiamiento registrados**: 0.
- **Proveedores registrados**: 0.
- **Diagnóstico de red**: prueba directa con `curl` contra
  `https://api.mercadopublico.cl/APISOCDS/OCDS/listaOCDSAgnoMes/2026/08/0/1`
  devuelve `curl: (56) CONNECT tunnel failed, response 403`. El diagnóstico
  del proxy (`$HTTPS_PROXY/__agentproxy/status`, disponible esta noche a
  diferencia de anoche) confirma la causa exacta:

  ```json
  "recentRelayFailures": [{
    "kind": "connect_rejected",
    "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
    "host": "api.mercadopublico.cl:443"
  }]
  ```

  Es decir: no es un problema de certificados, DNS ni de la API de
  ChileCompra — es la política de salida del entorno (`agent proxy`) la que
  rechaza explícitamente las conexiones a `api.mercadopublico.cl:443`.
  Idéntico a las 38 noches anteriores. Treinta y nueve noches seguidas con el
  mismo bloqueo de fondo.
- **`mpagente demanda`**: se corrió completa (`--meses 2 --hilos 4`, con
  `timeout 100` en vez de esperar los ~90 minutos porque el resultado ya
  estaba confirmado por el diagnóstico directo), agotó reintentos en ambos
  meses (2026-08 y 2026-09) y guardó 0 llamados.
- **`mpagente pidiendo`**: confirmó "No hay llamados recolectados"; esperado
  dado que `demanda` reportó 0.
- **Tests**: `.venv/bin/python -m pytest -q` → 58 pasan, 6 fallan, 6 dan
  error, todos por el fixture `data/mock/licitaciones.json` ausente (nunca
  se commiteó, está en `.gitignore`). Mismo resultado que noches anteriores.
  Afecta solo al modo "licitaciones abiertas" (en pausa); no afecta demanda
  ni nichos.
- **Export de contexto**: se re-exporta igual al final, solo cambia la marca
  de tiempo, para no perder el histórico ya cargado.
- **`git` — hallazgo de esta noche**: al arrancar, `HEAD` estaba *detached*
  en `2d1e911` (el commit del reporte de 2026-09-18), y tanto la rama local
  `main` como `origin/main` estaban atrasadas dos commits, todavía en
  `fdea363` (2026-09-16). Es decir, **los reportes de 2026-09-17 y
  2026-09-18 nunca habían llegado a `origin/main`**, a pesar de que ambos
  reportes decían "se commitea y empuja normalmente". Se hizo
  `git checkout main && git merge --ff-only 2d1e911 && git push`, y se
  confirmó con `git fetch` que `origin/main` quedó en `2d1e911`. No se puede
  saber desde acá si el push de esas dos noches falló silenciosamente por el
  mismo tipo de bloqueo de red, o si la sesión terminó antes de confirmar el
  push; vale la pena revisar los logs de esas corridas si están disponibles.
  El commit de esta noche se agrega sobre esa base ya sincronizada.

## Nota sobre esta noche en particular

Este es un mensaje explícito para quien lee el reporte, no un hallazgo de
negocio: el bloqueo de red lleva más de cinco semanas seguidas (39 noches) y
es exactamente el mismo error de política de proxy cada vez. Si nadie ha
podido o querido ajustar la configuración de red del entorno en la nube
donde corre esta rutina, seguir corriéndola todas las noches solo va a
seguir produciendo el mismo reporte vacío. Vale la pena decidir entre: (a)
ajustar la política de salida del entorno para permitir
`api.mercadopublico.cl`, o (b) pausar la rutina hasta que eso se resuelva,
para no acumular más noches idénticas.
