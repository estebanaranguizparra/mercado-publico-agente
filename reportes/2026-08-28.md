# Reporte nocturno — 2026-08-28

## Titular

Decimoséptima noche seguida sin datos nuevos: el entorno en la nube sigue sin
poder llegar a `api.mercadopublico.cl`. Mismo bloqueo de política de egreso
de las dieciséis noches anteriores (12 al 27 de agosto); el mensaje de
rechazo cambió de redacción pero no de causa.

## Lo accionable ahora

Nada de licitaciones: sin acceso a la API no hay llamados que recolectar ni
oportunidades nuevas que evaluar. Las 20 oportunidades ya registradas (10
`analizada`, 9 `descartada`, 1 `ofertada`) siguen exactamente donde estaban
hace diecisiete noches.

Lo único accionable sigue siendo de infraestructura: agregar
`api.mercadopublico.cl` al allowlist de egreso de red del entorno, tal como
ya se pidió la noche pasada.

## Por qué

No aplica a licitaciones: no se evaluó ninguna oportunidad nueva porque no
hubo datos nuevos que procesar.

## Descartes

Ninguno nuevo.

## Qué falta confirmar

- **El bloqueo sigue siendo el mismo, solo cambió la redacción del
  mensaje.** La noche pasada (2026-08-27) el proxy respondía con el header
  `x-deny-reason: host_not_allowed` y el texto "Host not in allowlist:
  api.mercadopublico.cl". Hoy el `CONNECT` directo a
  `api.mercadopublico.cl:443` (leído a nivel de socket, porque `curl` no
  expone el cuerpo cuando el túnel falla) devuelve:

  ```
  HTTP/1.1 403 Forbidden
  Content-Type: text/plain; charset=utf-8

  request blocked: no rule or allowlist entry allows host "api.mercadopublico.cl"
  ```

  Es el mismo diagnóstico de fondo que anoche —el host sigue sin estar en
  el allowlist de egreso del entorno— con una frase distinta. No es una
  mejora ni un empeoramiento, así que no amerita una alerta nueva además de
  la ya escalada anoche: sería repetir el mismo pedido con otras palabras.
  El diagnóstico del proxy (`$HTTPS_PROXY/__agentproxy/status`) sigue sin
  fallas nuevas en `recentRelayFailures` (vacío) y sin
  `installedProxyPreconfiguredClis`; el bloqueo ocurre en una capa de
  política de red distinta al proxy mismo, tal como se estableció anoche.
  - **Se corrió igual `mpagente demanda --meses 2 --hilos 4`** (con
    `timeout 100`): agotó reintentos contra `listaOCDSAgnoMes/2026/08/0/500`
    y `.../2026/07/0/500`, página vacía en ambos, 0 llamados guardados.
    Mismo resultado que las dieciséis noches anteriores.
  - Sigue pendiente la corrección en
    [claude.ai/code/routines](https://claude.ai/code/routines)
    (`trig_01TcKRcAxJJAfCwXrVPrPMLu`): agregar `api.mercadopublico.cl` al
    allowlist de egreso de red del entorno. Nadie con acceso a esa
    configuración la ha tocado todavía, a juzgar por que el rechazo persiste
    idéntico en su causa.
- Sigue sin resolverse la brecha de tests reportada las dieciséis noches
  anteriores: 58 pasan, 6 fallan y 6 dan error por
  `FileNotFoundError: data/mock/licitaciones.json`, un fixture en
  `.gitignore` que nunca se commiteó. No relacionado con el bloqueo de red
  — solo confirma que un clon fresco no lo trae. Afecta solo al modo
  "licitaciones abiertas", en pausa según `CLAUDE.md`; no afecta demanda ni
  nichos. Sigue pendiente decidir si se commitea el fixture o se documenta
  como gap conocido.

## Estado del sistema

- **Contexto importado correctamente**: 314 familias, 20 oportunidades, 22
  eventos (generado 2026-08-27T06:16:44, sin cambios desde ayer porque no
  hubo datos nuevos que agregar).
- **Embudo** (sin cambios): `analizada` 10 · `descartada` 9 · `ofertada` 1.
  Ofertadas 1, ganadas 0.
- **Garantías e instrumentos de financiamiento registrados**: 0.
- **Proveedores registrados**: 0.
- **Diagnóstico de red**: `CONNECT api.mercadopublico.cl:443` vía el proxy
  configurado → `HTTP/1.1 403 Forbidden`, cuerpo `request blocked: no rule
  or allowlist entry allows host "api.mercadopublico.cl"`. Mismo bloqueo de
  fondo que las dieciséis noches previas (11 con `CONNECT tunnel failed,
  response 403` sin razón declarada, la última con `x-deny-reason:
  host_not_allowed`), ahora con una tercera redacción del mismo rechazo.
- **`mpagente demanda`**: se corrió completa (con `timeout 100` en vez de
  esperar los ~90 minutos), agotó reintentos en ambos meses (403 en cada
  petición) y guardó 0 llamados.
- **`mpagente pidiendo`**: respondió "No hay llamados recolectados",
  consistente con 0 llamados guardados (la base de 96 MB del Mac tampoco
  está disponible en este entorno).
- **Tests**: `.venv/bin/python -m pytest -q` → 58 pasan, 6 fallan, 6 dan
  error, todos por el fixture `data/mock/licitaciones.json` ausente. Mismo
  resultado que las dieciséis noches anteriores.
- **Export de contexto**: se re-exporta igual al final, solo cambia la
  marca de tiempo, para no perder el histórico ya cargado.
