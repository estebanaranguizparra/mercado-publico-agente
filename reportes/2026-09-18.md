# Reporte nocturno — 2026-09-18

## Titular

Noche 38 seguida sin datos nuevos: el entorno en la nube sigue sin poder
llegar a `api.mercadopublico.cl`. El rechazo es el mismo, byte a byte, que
hace más de un mes.

## Lo accionable ahora

Nada de licitaciones: sin acceso a la API no hay llamados que recolectar ni
oportunidades nuevas que evaluar. Las 20 oportunidades ya registradas (10
`analizada`, 9 `descartada`, 1 `ofertada`) siguen exactamente donde estaban
hace treinta y ocho noches.

Lo único accionable sigue siendo de infraestructura: agregar
`api.mercadopublico.cl` al allowlist de egreso de red del entorno en
[claude.ai/code/routines](https://claude.ai/code/routines)
(`trig_01TcKRcAxJJAfCwXrVPrPMLu`). Nadie con acceso a esa configuración la ha
tocado en treinta y ocho noches, a juzgar por que el rechazo persiste
idéntico.

No hay novedad de infraestructura de git: la sesión partió con `HEAD`
apuntando a `aa5d4ab` (el commit del reporte de anoche) pero en estado
*detached*, un episodio distinto y más leve que los de noches anteriores
—`origin/main` ya tenía ese mismo commit, así que no hubo nada que
recuperar—. Se resolvió con `git checkout main && git merge --ff-only
origin/main`, igual que la vez anterior. Vale seguir vigilando si vuelve a
aparecer.

## Por qué

No aplica a licitaciones: no se evaluó ninguna oportunidad nueva porque no
hubo datos nuevos que procesar.

## Descartes

Ninguno nuevo.

## Qué falta confirmar

- **El bloqueo de red es exactamente el mismo que las treinta y siete
  noches anteriores.** Una prueba directa con `curl` contra
  `https://api.mercadopublico.cl/APISOCDS/OCDS/listaOCDSAgnoMes/2026/08/0/1`
  devuelve:

  ```
  curl: (56) CONNECT tunnel failed, response 403
  ```

  Se corrió igual `mpagente demanda --meses 2 --hilos 4` (con `timeout
  100`): agotó reintentos contra `listaOCDSAgnoMes/2026/09/0/500` y
  `.../2026/08/0/500`, página vacía en ambos, 0 llamados guardados. Mismo
  resultado que las treinta y siete noches anteriores. Esta vez no se pudo
  además consultar el diagnóstico del proxy
  (`$HTTPS_PROXY/__agentproxy/status`): el clasificador de modo automático
  denegó el comando por considerarlo posible reconocimiento de exfiltración.
  No cambia el diagnóstico de fondo —el 403 directo contra la API ya lo
  confirma— pero es la primera noche en que ese chequeo puntual no se pudo
  correr; si vuelve a bloquearse, hay que decidir si conviene quitarlo del
  checklist.
- Sigue sin resolverse la brecha de tests reportada las treinta y siete
  noches anteriores: con `.[dev,bases]` instalado en un venv fresco, 58
  pasan, 6 fallan y 6 dan error por `FileNotFoundError:
  data/mock/licitaciones.json`, un fixture en `.gitignore` que nunca se
  commiteó. No relacionado con el bloqueo de red — solo confirma que un
  clon fresco no lo trae. Afecta solo al modo "licitaciones abiertas", en
  pausa según `CLAUDE.md`; no afecta demanda ni nichos. Sigue pendiente
  decidir si se commitea el fixture o se documenta como gap conocido.
- La instalación inicial de esta noche fue con `.venv/bin/pip install -q -e
  ".[bases]"`, que no trae `pytest`; hubo que reinstalar con
  `.[dev,bases]` para poder correr los tests. Vale ajustar el paso 1 de la
  rutina para pedir `.[dev,bases]` desde el principio y ahorrarse la
  segunda instalación.

## Estado del sistema

- **Contexto importado correctamente**: 314 familias, 20 oportunidades, 22
  eventos (generado 2026-09-17T06:08:57 según el `contexto.json` del repo —
  el timestamp interno no cambia porque no hubo datos nuevos que agregar).
- **Embudo** (sin cambios): `analizada` 10 · `descartada` 9 · `ofertada` 1.
  Ofertadas 1, ganadas 0.
- **Garantías e instrumentos de financiamiento registrados**: 0.
- **Proveedores registrados**: 0.
- **Diagnóstico de red**: `CONNECT api.mercadopublico.cl:443` → `403`,
  idéntico en sustancia a las treinta y siete noches anteriores. Treinta y
  ocho noches seguidas con el mismo bloqueo de fondo.
- **`mpagente demanda`**: se corrió completa (con `timeout 100` en vez de
  esperar los ~90 minutos), agotó reintentos en ambos meses (403 en cada
  petición) y guardó 0 llamados.
- **`mpagente pidiendo`**: confirmó "No hay llamados recolectados"; el
  resultado esperado dado que `demanda` reportó 0 llamados.
- **Tests**: `.venv/bin/python -m pytest -q` → 58 pasan, 6 fallan, 6 dan
  error, todos por el fixture `data/mock/licitaciones.json` ausente. Mismo
  resultado que las treinta y siete noches anteriores.
- **Export de contexto**: se re-exporta igual al final, solo cambia la
  marca de tiempo, para no perder el histórico ya cargado.
- **`git`**: `HEAD` llegó *detached* apuntando al commit de anoche
  (`aa5d4ab`), ya presente en `origin/main`; se reencauzó a `main` con
  `checkout` + `merge --ff-only` sin pérdida de datos. Este reporte se
  commitea y empuja normalmente sobre `main`.
