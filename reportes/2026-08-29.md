# Reporte nocturno — 2026-08-29

## Titular

Decimoctava noche seguida sin datos nuevos: el entorno en la nube sigue sin
poder llegar a `api.mercadopublico.cl`. Mismo bloqueo de política de egreso
de red de las diecisiete noches anteriores (12 al 28 de agosto), mensaje de
rechazo idéntico, letra por letra, al de anoche.

## Lo accionable ahora

Nada de licitaciones: sin acceso a la API no hay llamados que recolectar ni
oportunidades nuevas que evaluar. Las 20 oportunidades ya registradas (10
`analizada`, 9 `descartada`, 1 `ofertada`) siguen exactamente donde estaban
hace dieciocho noches.

Lo único accionable sigue siendo de infraestructura: agregar
`api.mercadopublico.cl` al allowlist de egreso de red del entorno en
[claude.ai/code/routines](https://claude.ai/code/routines)
(`trig_01TcKRcAxJJAfCwXrVPrPMLu`). Nadie con acceso a esa configuración la
ha tocado en dieciocho noches, a juzgar por que el rechazo persiste
idéntico.

## Por qué

No aplica a licitaciones: no se evaluó ninguna oportunidad nueva porque no
hubo datos nuevos que procesar.

## Descartes

Ninguno nuevo.

## Qué falta confirmar

- **El bloqueo es exactamente el mismo que anoche, sin variación.** El
  `CONNECT` directo a `api.mercadopublico.cl:443` (leído a nivel de socket,
  porque `curl` no expone el cuerpo cuando el túnel falla) devuelve:

  ```
  HTTP/1.1 403 Forbidden
  Content-Type: text/plain; charset=utf-8

  request blocked: no rule or allowlist entry allows host "api.mercadopublico.cl"
  ```

  Byte por byte igual al de anoche (2026-08-28). El diagnóstico del proxy
  (`$HTTPS_PROXY/__agentproxy/status`) tampoco cambió: `recentRelayFailures`
  vacío, `installedProxyPreconfiguredClis` vacío, `selective: false`. El
  bloqueo sigue ocurriendo en una capa de política de red del entorno,
  distinta al proxy mismo, y nadie la ha tocado en dieciocho noches.
  - **Se corrió igual `mpagente demanda --meses 2 --hilos 4`** (con
    `timeout 100`): agotó reintentos contra `listaOCDSAgnoMes/2026/08/0/500`
    y `.../2026/07/0/500`, página vacía en ambos, 0 llamados guardados.
    Mismo resultado que las diecisiete noches anteriores.
  - Dado que el mensaje no cambia noche tras noche desde hace más de dos
    semanas, este reporte lo deja escrito una vez más pero **se avisa por
    notificación aparte**, porque diecisiete reportes seguidos señalando lo
    mismo sin que nadie lo haya corregido sugiere que el reporte solo no
    basta como canal para este pendiente.
- Sigue sin resolverse la brecha de tests reportada las diecisiete noches
  anteriores: 58 pasan, 6 fallan y 6 dan error por
  `FileNotFoundError: data/mock/licitaciones.json`, un fixture en
  `.gitignore` que nunca se commiteó. No relacionado con el bloqueo de red
  — solo confirma que un clon fresco no lo trae. Afecta solo al modo
  "licitaciones abiertas", en pausa según `CLAUDE.md`; no afecta demanda ni
  nichos. Sigue pendiente decidir si se commitea el fixture o se documenta
  como gap conocido.

## Estado del sistema

- **Contexto importado correctamente**: 314 familias, 20 oportunidades, 22
  eventos (generado 2026-08-28T06:15:49, sin cambios desde ayer porque no
  hubo datos nuevos que agregar).
- **Embudo** (sin cambios): `analizada` 10 · `descartada` 9 · `ofertada` 1.
  Ofertadas 1, ganadas 0.
- **Garantías e instrumentos de financiamiento registrados**: 0.
- **Proveedores registrados**: 0.
- **Diagnóstico de red**: `CONNECT api.mercadopublico.cl:443` vía el proxy
  configurado → `HTTP/1.1 403 Forbidden`, cuerpo `request blocked: no rule
  or allowlist entry allows host "api.mercadopublico.cl"`. Idéntico, byte a
  byte, al de anoche. Dieciocho noches seguidas con el mismo bloqueo de
  fondo.
- **`mpagente demanda`**: se corrió completa (con `timeout 100` en vez de
  esperar los ~90 minutos), agotó reintentos en ambos meses (403 en cada
  petición) y guardó 0 llamados.
- **`mpagente pidiendo`**: respondió "No hay llamados recolectados",
  consistente con 0 llamados guardados (la base de 96 MB del Mac tampoco
  está disponible en este entorno).
- **Tests**: `.venv/bin/python -m pytest -q` → 58 pasan, 6 fallan, 6 dan
  error, todos por el fixture `data/mock/licitaciones.json` ausente. Mismo
  resultado que las diecisiete noches anteriores.
- **Export de contexto**: se re-exporta igual al final, solo cambia la
  marca de tiempo, para no perder el histórico ya cargado.
