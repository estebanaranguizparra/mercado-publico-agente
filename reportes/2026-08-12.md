# Reporte nocturno — 2026-08-12

## Titular

La corrida de esta noche no pudo recolectar nada: el entorno en la nube tiene
bloqueado por política de red el dominio completo `mercadopublico.cl`, no solo
limitado por tasa. No hay oportunidades nuevas que reportar porque no se pudo
consultar la API.

## Lo accionable ahora

Nada. Sin datos nuevos no hay tabla que armar. Las 10 oportunidades marcadas
`analizada` en corridas anteriores (ver estado del sistema) siguen donde
estaban; no se pudo revisar si siguen vigentes.

## Por qué

No aplica: no se evaluó ninguna oportunidad nueva.

## Descartes

Ninguno nuevo.

## Qué falta confirmar

- **Si el bloqueo de red es intencional o un error de configuración del
  entorno.** `curl` contra `api.mercadopublico.cl:443` devuelve `403` en el
  `CONNECT` del proxy con motivo `"policy denial or upstream failure"`, y
  `WebFetch` contra `www.mercadopublico.cl` responde directamente
  `EGRESS_BLOCKED`. Esto es distinto al límite de 5 req/s ya conocido y
  documentado en `CLAUDE.md`: ahí la API respondía (con 429 si se pasaba el
  límite); acá el proxy de salida de este entorno rechaza la conexión antes
  de llegar a la API, para *todas* las solicitudes.
- Mientras el dominio esté bloqueado, ningún subcomando que dependa de red
  (`demanda`, `recolectar`, `lector-bases`, los buscadores de proveedores)
  puede correr en este entorno. Si el bloqueo es deliberado, esta rutina
  nocturna necesita moverse a un entorno con `api.mercadopublico.cl` y
  `www.mercadopublico.cl` permitidos en la política de egreso, o quedar en
  pausa hasta que se habilite.
- Si el bloqueo fue accidental, ajustar la política de red del entorno
  (`claude.ai/code/routines` → configuración del entorno de esta rutina) y
  volver a intentarlo la próxima noche.

## Estado del sistema

- **Contexto importado correctamente**: 314 familias, 20 oportunidades, 22
  eventos (generado 2026-08-11T11:42:36 en el Mac).
- **Embudo heredado** (sin cambios esta noche): `analizada` 10 ·
  `descartada` 9 · `ofertada` 1. Ofertadas 1, ganadas 0.
- **Garantías e instrumentos de financiamiento registrados**: 0. No se buscó
  ninguno nuevo porque no hubo oportunidades nuevas que lo requirieran.
- **Proveedores registrados**: 0.
- **`mpagente demanda --meses 2 --hilos 4`**: 0 procesos revisados, 0
  llamados guardados. Falló en ambos meses (2026-07 y 2026-08) por el
  bloqueo de red descrito arriba, no por el mes en curso (que se espera que
  falle) ni por el límite de tasa.
- **`mpagente pidiendo`**: "No hay llamados recolectados" — consistente con
  lo anterior; este entorno no trae la base de 96 MB del Mac, solo el
  contexto portable, que no incluye llamados individuales.
- **Tests**: `.venv/bin/python -m pytest -q` → 58 pasan, 6 fallan y 6 dan
  error, todos por `FileNotFoundError: data/mock/licitaciones.json`.
  `data/mock/` está en `.gitignore` y ese archivo nunca se commiteó, así que
  un clon fresco (como el de esta corrida) no lo tiene. Es una brecha
  preexistente del repo, no algo que haya causado esta corrida — pero vale
  la pena que alguien decida si el fixture debe generarse en un
  `conftest.py` o versionarse.
- **Export de contexto**: se re-exporta igual al final, sin cambios de
  contenido más allá de la marca de tiempo, para no perder el histórico ya
  cargado.
