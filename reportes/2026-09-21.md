# Reporte nocturno — 2026-09-21

Noche 41 del mismo bloqueo de red de fondo. Sin llamados nuevos que
analizar; el embudo de oportunidades queda igual que ayer.

## 1. Lo accionable ahora

Sin novedades. `mpagente demanda --meses 2 --hilos 4` guardó 0 llamados (ver
diagnóstico abajo), así que `mpagente pidiendo` no tuvo nada que triage:
respondió "No hay llamados recolectados". Las 10 oportunidades `analizada`
de noches anteriores (discos duros, radiocomunicación, unidades de datos,
TI/ISP, software y licencias, impresoras, formación y capacitación,
asesorías de gestión, producción de eventos, gasfitería) siguen abiertas
para retomar en cuanto la red funcione; no se reevaluaron porque no hay
datos nuevos que cambien su motivo registrado.

## 2. Por qué (nada que analizar)

No aplica: cero llamados nuevos, cero bases leídas, cero decisiones nuevas.

## 3. Descartes

Ninguno nuevo. Los 9 descartes previos (telefonía móvil, internet, seguros,
tres licitaciones LR por monto de garantía, una por plazo insuficiente de 7
días, una por barrera dura de fabricante, más la que corresponda) siguen
vigentes con su motivo original.

## 4. Qué falta confirmar

- **El bloqueo de red**, que ya lleva 41 noches seguidas (casi seis
  semanas). Sigue siendo exactamente el mismo rechazo de política de proxy
  contra `api.mercadopublico.cl:443` (ver estado del sistema). Necesita que
  alguien con acceso a la configuración de red del entorno en la nube
  agregue ese host a la política de salida, o que se decida pausar la
  rutina mientras tanto.
- El precio de referencia y la competencia que arrastra `contexto.json`
  siguen firmes en su fecha de origen (histórico de adjudicaciones parcial,
  como advierte `CLAUDE.md`), y llevan 41 días sin poder refrescarse.
- La brecha de tests por el fixture `data/mock/licitaciones.json` ausente
  sigue sin decidirse (¿commitear el fixture o documentar como gap conocido
  del modo "licitaciones abiertas", en pausa?). No urgente.

## 5. Estado del sistema

- **Contexto importado correctamente**: 314 familias, 20 oportunidades, 22
  eventos.
- **Embudo** (sin cambios): `analizada` 10 · `descartada` 9 · `ofertada` 1.
  Ofertadas 1, ganadas 0.
- **Garantías e instrumentos de financiamiento registrados**: 0.
- **Proveedores registrados**: 0.
- **Diagnóstico de red**: prueba directa con `curl` contra
  `https://api.mercadopublico.cl/APISOCDS/OCDS/listaOCDSAgnoMes/2026/8/1/1`
  devuelve `curl: (56) CONNECT tunnel failed, response 403`. El diagnóstico
  del proxy (`$HTTPS_PROXY/__agentproxy/status`) confirma la causa exacta:

  ```json
  "recentRelayFailures": [{
    "kind": "connect_rejected",
    "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
    "host": "api.mercadopublico.cl:443"
  }]
  ```

  Es la política de salida del entorno (`agent proxy`) la que rechaza
  explícitamente las conexiones a `api.mercadopublico.cl:443`, no un
  problema de certificados, DNS ni de la API de ChileCompra. Idéntico a las
  40 noches anteriores.
- **`mpagente demanda`**: se corrió completa (`--meses 2 --hilos 4`),
  agotó reintentos en ambos meses (2026-08 y 2026-09) y guardó 0 llamados.
- **`mpagente pidiendo`**: confirmó "No hay llamados recolectados"; esperado
  dado que `demanda` reportó 0.
- **Tests**: `.venv/bin/python -m pytest -q` → 58 pasan, 6 fallan, 6 dan
  error, todos por el fixture `data/mock/licitaciones.json` ausente (nunca
  se commiteó, está en `.gitignore`). Mismo resultado que noches anteriores.
  Afecta solo al modo "licitaciones abiertas" (en pausa); no afecta demanda
  ni nichos.
- **Export de contexto**: se re-exporta igual al final, solo cambia la marca
  de tiempo, para no perder el histórico ya cargado.
- **`git`**: el sandbox arrancó sin `.venv` (contenedor efímero nuevo) y con
  `HEAD` detached en `3203deb` (el commit del reporte de 2026-09-20). La
  rama local `main` estaba atrasada en `fdea363` (2026-09-16); se hizo
  `git checkout main && git merge --ff-only origin/main` para ponerla al
  día — el push de anoche sí había llegado a `origin/main`. El commit de
  esta noche se agrega sobre esa base ya sincronizada.

## Nota sobre esta noche en particular

El bloqueo de red lleva 41 noches seguidas (casi seis semanas) y es
exactamente el mismo error de política de proxy cada vez. Sigue en pie la
recomendación de noches anteriores: (a) ajustar la política de salida del
entorno para permitir `api.mercadopublico.cl`, o (b) pausar la rutina hasta
que eso se resuelva, para no seguir acumulando reportes idénticos.
