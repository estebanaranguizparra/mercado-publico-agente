# Reporte nocturno — 2026-08-24

## Titular

Decimotercera noche seguida sin datos nuevos: el entorno en la nube sigue
sin poder llegar a `api.mercadopublico.cl`. Mismo bloqueo de política de
egreso de las doce noches anteriores (12 al 23 de agosto), sin ningún
cambio.

## Lo accionable ahora

Nada. Sin acceso a la API no hay llamados que recolectar ni oportunidades
nuevas que evaluar. Las 20 oportunidades ya registradas (10 `analizada`, 9
`descartada`, 1 `ofertada`) siguen exactamente donde estaban hace trece
noches.

## Por qué

No aplica: no se evaluó ninguna oportunidad nueva porque no hubo datos
nuevos que procesar.

## Descartes

Ninguno nuevo.

## Qué falta confirmar

- **El bloqueo lleva trece noches seguidas, sin cambio en la política.**
  `curl -v https://api.mercadopublico.cl/...` sigue devolviendo `CONNECT
  tunnel failed, response 403`:

  ```
  < HTTP/1.1 403 Forbidden
  * CONNECT tunnel failed, response 403
  ```

  El diagnóstico del proxy (`curl -sS "$HTTPS_PROXY/__agentproxy/status"`)
  esta vez sí registró el rechazo en `recentRelayFailures`:

  ```json
  {
    "ts": "2026-08-24T06:13:40.000Z",
    "kind": "connect_rejected",
    "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
    "host": "api.mercadopublico.cl:443"
  }
  ```

  Se corrió igual `mpagente demanda --meses 2 --hilos 4` (con `timeout 90`
  en vez de esperar los ~90 minutos completos): agotó los reintentos contra
  `listaOCDSAgnoMes/2026/08/0/500` y `.../2026/07/0/500`, devolvió página
  vacía en ambos meses y terminó con "0 llamados guardados". Es la misma
  **denegación de política de egreso** del entorno de las doce noches
  anteriores, no un problema transitorio ni del lado de la API de
  ChileCompra.
- Con trece noches consecutivas del mismo bloqueo y sin señal de cambio,
  esta corrida sigue sin aportar información nueva sobre el problema —
  solo confirma que persiste. Ya se escaló esta situación en noches
  anteriores a quien administra
  [claude.ai/code/routines](https://claude.ai/code/routines)
  (`trig_01TcKRcAxJJAfCwXrVPrPMLu`), así que no se envía una notificación
  nueva por esto: sería repetir la misma alerta sin novedad. Si la política
  de egreso se resuelve o cambia, la próxima corrida lo va a detectar y
  notificar de inmediato.
- Sigue sin resolverse la brecha de tests reportada las doce noches
  anteriores: 58 pasan, 6 fallan y 6 dan error por `FileNotFoundError:
  data/mock/licitaciones.json`, un fixture en `.gitignore` que nunca se
  commiteó. No relacionado con el bloqueo de red — solo confirma que un
  clon fresco no lo trae. Afecta solo al modo "licitaciones abiertas", en
  pausa según `CLAUDE.md`; no afecta demanda ni nichos. Sigue pendiente
  decidir si se commitea el fixture o se documenta como gap conocido.

## Estado del sistema

- **Contexto importado correctamente**: 314 familias, 20 oportunidades, 22
  eventos (generado 2026-08-23T06:08:16 según el commit previo, sin
  cambios desde hace trece noches porque no hubo datos nuevos que
  agregar).
- **Embudo** (sin cambios): `analizada` 10 · `descartada` 9 · `ofertada`
  1. Ofertadas 1, ganadas 0.
- **Garantías e instrumentos de financiamiento registrados**: 0.
- **Proveedores registrados**: 0.
- **Diagnóstico de red**: `curl -v https://api.mercadopublico.cl/...` →
  `CONNECT tunnel failed, response 403`. `mpagente demanda` se corrió
  completa (con `timeout 90` en vez de esperar los ~90 minutos), agotó
  reintentos en ambos meses y guardó 0 llamados, contra el mismo bloqueo.
- **`mpagente pidiendo`**: no se ejecutó; sin llamados recolectados no
  habría nada que mostrar (la base de 96 MB del Mac tampoco está
  disponible en este entorno).
- **Tests**: `.venv/bin/python -m pytest -q` → 58 pasan, 6 fallan, 6 dan
  error, todos por el fixture `data/mock/licitaciones.json` ausente.
  Mismo resultado que las doce noches anteriores.
- **Export de contexto**: se re-exporta igual al final, solo cambia la
  marca de tiempo, para no perder el histórico ya cargado.
